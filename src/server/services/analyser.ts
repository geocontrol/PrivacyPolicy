// LLM-powered policy analyser
// Sends retrieved policy text to the configured LLM provider and returns
// structured analysis results (summary, data categories, third parties, etc.)

export async function analysePolicy(_policyText: string): Promise<Record<string, unknown>> {
  throw new Error('Not implemented yet')
}
